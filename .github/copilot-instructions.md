# Repository Instructions

Use `AGENTS.md` and `.ai/` as the authoritative engineering context.

Preserve domain boundaries, tenant isolation, strict typing, async I/O, repository/unit-of-work patterns, and the implemented-versus-planned distinction. Run `make check` for code changes.

